export function applyBlockModifications(initial, { add = [], exclude = [] } = {}) {
    if (!add.length && !exclude.length) {
        return initial;
    }
    const blocks = new Set(initial);
    for (const added of add) {
        blocks.add(added);
    }
    for (const excluded of exclude) {
        blocks.delete(excluded);
    }
    return Array.from(blocks);
}
//# sourceMappingURL=applyBlockModifications.js.map