import { Block } from "../types/blocks.js";
import { CreatedBlockAddons, Creation } from "../types/creations.js";
import { ProductionMode } from "../types/modes.js";
import { SystemContext } from "../types/system.js";
export interface ProduceBlocksSettings<Options extends object> {
    addons?: CreatedBlockAddons<object, Options>[];
    mode?: ProductionMode;
    offline?: boolean;
    options: Options;
    system: SystemContext;
}
export declare function produceBlocks<Options extends object>(blocks: Block<object | undefined, Options>[], { addons, mode, offline, options, system }: ProduceBlocksSettings<Options>): Partial<Creation<Options>>;
