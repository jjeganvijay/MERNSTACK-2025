export function applyBlockModifications(initial, { add = [], remove = [] } = {}) {
    if (!add.length && !remove.length) {
        return initial;
    }
    const blocks = new Set(initial);
    for (const added of add) {
        blocks.add(added);
    }
    for (const removed of remove) {
        blocks.delete(removed);
    }
    return Array.from(blocks);
}
//# sourceMappingURL=applyPresetBlockModifications.js.map