import { isPreset } from "./isPreset.js";
export function isCreateConfig(value) {
    return (!!value &&
        typeof value === "object" &&
        "preset" in value &&
        isPreset(value.preset));
}
//# sourceMappingURL=isCreateConfig.js.map