import { isBase } from "./isBase.js";
import { isPresetAbout } from "./isPresetAbout.js";
export function isPreset(value) {
    return (!!value &&
        typeof value === "object" &&
        "about" in value &&
        isPresetAbout(value.about) &&
        "base" in value &&
        isBase(value.base) &&
        "blocks" in value &&
        Array.isArray(value.blocks));
}
//# sourceMappingURL=isPreset.js.map