import { BlockModifications } from "../config/types.js";
import { AnyShape, InferredObject } from "../options.js";
import { CreatedBlockAddons, Creation } from "../types/creations.js";
import { ProductionMode } from "../types/modes.js";
import { Preset } from "../types/presets.js";
import { NativeSystem } from "../types/system.js";
export interface RunPresetSettings<OptionsShape extends AnyShape> extends RunSettingsBase {
    addons?: CreatedBlockAddons<object, InferredObject<OptionsShape>>[];
    blocks?: BlockModifications<InferredObject<OptionsShape>>;
    offline?: boolean;
    options: InferredObject<OptionsShape>;
}
export interface RunSettingsBase extends Partial<NativeSystem> {
    auth?: string;
    directory?: string;
    mode?: ProductionMode;
    offline?: boolean;
}
export declare function runPreset<OptionsShape extends AnyShape>(preset: Preset<OptionsShape>, settings: RunPresetSettings<OptionsShape>): Promise<Creation<InferredObject<OptionsShape>>>;
