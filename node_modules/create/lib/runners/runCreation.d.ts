import { DirectCreation } from "../types/creations.js";
import { SystemContext } from "../types/system.js";
export interface ApplyCreationSettings {
    offline?: boolean;
    system: SystemContext;
}
export declare function runCreation(creation: Partial<DirectCreation>, { offline, system }: ApplyCreationSettings): Promise<void>;
