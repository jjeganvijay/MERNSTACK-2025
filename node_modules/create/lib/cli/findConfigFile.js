import * as fs from "node:fs/promises";
export async function findConfigFile(directory) {
    try {
        return (await fs.readdir(directory)).find(isConfigFileName);
    }
    catch {
        return undefined;
    }
}
export function isConfigFileName(fileName) {
    return /create\.config\.\w+/.test(fileName);
}
//# sourceMappingURL=findConfigFile.js.map