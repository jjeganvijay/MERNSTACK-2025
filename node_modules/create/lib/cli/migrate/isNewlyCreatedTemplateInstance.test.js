import { describe, expect, it, vi } from "vitest";
import { isNewlyCreatedTemplateInstance } from "./isNewlyCreatedTemplateInstance.js";
const mockFromUrl = vi.fn();
vi.mock("hosted-git-info", () => ({
    default: {
        get fromUrl() {
            return mockFromUrl;
        },
    },
}));
const mockReadPackage = vi.fn();
vi.mock("read-pkg", () => ({
    get readPackage() {
        return mockReadPackage;
    },
}));
const template = {
    owner: "TestOwner",
    repository: "test-repository",
};
describe("createRepositoryOnGitHub", () => {
    it("returns false when there is no package repository url", async () => {
        mockReadPackage.mockResolvedValueOnce({});
        const actual = await isNewlyCreatedTemplateInstance(".", template);
        expect(actual).toBe(false);
        expect(mockFromUrl).not.toHaveBeenCalled();
    });
    it("returns false when the Git repository doesn't have information", async () => {
        mockReadPackage.mockResolvedValueOnce({
            repository: { url: "..." },
        });
        mockFromUrl.mockResolvedValueOnce(undefined);
        const actual = await isNewlyCreatedTemplateInstance(".", template);
        expect(actual).toBe(false);
    });
    it("returns false when the Git repository user doesn't match the template owner", async () => {
        mockReadPackage.mockResolvedValueOnce({
            repository: { url: "..." },
        });
        mockFromUrl.mockResolvedValueOnce({
            project: template.repository,
            user: "other",
        });
        const actual = await isNewlyCreatedTemplateInstance(".", template);
        expect(actual).toBe(false);
    });
    it("returns false when the Git repository project doesn't match the template repository", async () => {
        mockReadPackage.mockResolvedValueOnce({
            repository: { url: "..." },
        });
        mockFromUrl.mockResolvedValueOnce({
            project: "other",
            user: template.owner,
        });
        const actual = await isNewlyCreatedTemplateInstance(".", template);
        expect(actual).toBe(false);
    });
    it("returns true when the Git repository matches the template repository", async () => {
        mockReadPackage.mockResolvedValueOnce({
            repository: { url: "..." },
        });
        mockFromUrl.mockResolvedValueOnce({
            project: template.repository,
            user: template.owner,
        });
        const actual = await isNewlyCreatedTemplateInstance(".", template);
        expect(actual).toBe(true);
    });
});
//# sourceMappingURL=isNewlyCreatedTemplateInstance.test.js.map