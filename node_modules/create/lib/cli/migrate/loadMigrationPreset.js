import { tryImportConfig } from "../../config/tryImportConfig.js";
import { tryImportTemplatePreset } from "../importers/tryImportTemplatePreset.js";
export async function loadMigrationPreset({ configFile, from, requestedPreset, }) {
    if (configFile && from) {
        return new Error("--mode migrate requires either a config file or a specified template, but not both.");
    }
    if (configFile) {
        return await tryImportConfig(configFile);
    }
    if (from) {
        return await tryImportTemplatePreset(from, requestedPreset);
    }
    return new Error("--mode migrate requires either a config file exist or a template be specified on the CLI.");
}
//# sourceMappingURL=loadMigrationPreset.js.map