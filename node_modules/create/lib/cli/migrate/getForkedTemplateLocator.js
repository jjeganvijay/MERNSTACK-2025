import hostedGitInfo from "hosted-git-info";
import { readPackage } from "read-pkg";
export async function getForkedTemplateLocator(directory, template) {
    const { repository } = await readPackage({ cwd: directory });
    if (!repository?.url) {
        return undefined;
    }
    const gitInfo = hostedGitInfo.fromUrl(repository.url);
    if (gitInfo?.user !== template.owner ||
        gitInfo.project !== template.repository) {
        return undefined;
    }
    return `${template.owner}/${template.repository}`;
}
//# sourceMappingURL=getForkedTemplateLocator.js.map