import { SystemRunner } from "../types/system.js";
export interface CreateInitialCommitSettings {
    amend?: boolean;
    offline?: boolean;
}
export declare function createInitialCommit(runner: SystemRunner, { amend, offline }?: CreateInitialCommitSettings): Promise<void>;
