export function debounce(callback) {
    let requested = false;
    return async (...args) => {
        if (requested) {
            return;
        }
        requested = true;
        try {
            await callback(...args);
        }
        finally {
            requested = false;
        }
    };
}
//# sourceMappingURL=debounce.js.map