import { Template } from "../../types/templates.js";
export interface PromptForDirectorySettings {
    requestedDirectory?: string;
    requestedRepository?: string;
    template: Template;
}
export declare function promptForDirectory({ requestedRepository, requestedDirectory, template, }: PromptForDirectorySettings): Promise<string | symbol>;
