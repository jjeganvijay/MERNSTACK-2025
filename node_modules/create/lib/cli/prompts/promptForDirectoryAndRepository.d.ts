import { Template } from "../../types/templates.js";
export interface PromptForDirectorySettings {
    requestedDirectory: string | undefined;
    requestedRepository: string | undefined;
    template: Template;
}
export declare function promptForDirectoryAndRepository({ requestedDirectory, requestedRepository, template, }: PromptForDirectorySettings): Promise<string | symbol>;
