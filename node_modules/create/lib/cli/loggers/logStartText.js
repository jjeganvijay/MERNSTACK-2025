import * as prompts from "@clack/prompts";
import chalk from "chalk";
export function logStartText(mode, from, type, offline) {
    prompts.log.message([
        `Running with mode --${mode} using the ${type}:`,
        `  ${chalk.green(from)}`,
    ].join("\n"));
    if (offline) {
        prompts.log.message("--offline enabled. You'll need to git push any changes manually.");
    }
}
//# sourceMappingURL=logStartText.js.map