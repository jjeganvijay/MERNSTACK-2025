import * as prompts from "@clack/prompts";
import chalk from "chalk";
export function logHelpOptions(heading, options) {
    prompts.log.message([
        `${chalk.bgGreenBright.black(heading)} options:`,
        ...Object.entries(options).map(([heading, option]) => [
            chalk.green(`  --${heading} (${option.type}): ${option.text}`),
            chalk.blue(`    npx create ${option.example}`),
        ].join("\n")),
    ].join("\n\n"));
}
//# sourceMappingURL=logOptions.js.map