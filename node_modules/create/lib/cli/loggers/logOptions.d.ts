export interface HelpOption {
    example: string;
    text: string;
    type: string;
}
export type HelpOptions = Record<string, HelpOption>;
export declare function logHelpOptions(heading: string, options: HelpOptions): void;
