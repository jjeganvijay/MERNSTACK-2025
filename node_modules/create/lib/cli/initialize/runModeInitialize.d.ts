import { ClackDisplay } from "../display/createClackDisplay.js";
import { ModeResults } from "../types.js";
export interface RunModeInitializeSettings {
    args: string[];
    directory?: string;
    display: ClackDisplay;
    from?: string;
    help?: boolean;
    offline?: boolean;
    owner?: string;
    preset?: string;
    repository?: string;
}
export declare function runModeInitialize({ args, repository, directory: requestedDirectory, display, from, help, offline, preset: requestedPreset, }: RunModeInitializeSettings): Promise<ModeResults>;
