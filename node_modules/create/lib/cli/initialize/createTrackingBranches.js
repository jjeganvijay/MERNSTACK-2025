export async function createTrackingBranches({ owner, repository }, runner) {
    for (const command of [
        `git init`,
        `git remote add origin https://github.com/${owner}/${repository}`,
    ]) {
        await runner(command);
    }
}
//# sourceMappingURL=createTrackingBranches.js.map